doctype html
head
  title My Website
  style
    include ../css/style.css
.center
  h1 Hello!
  p Server UP!
  h1#current_time
    br
    script.
      document.write(new Date());
  h1
    br
  button(onclick="window.location='/msg'") msg
  =' '
  button(onclick="window.location='/json'") json
  =' '
  button(onclick="window.location='/sha256'") sha256
  script.
    function updateTime() {
        let d = new Date();
        document.getElementById('current_time').innerHTML = '<br/>' + d;
    }
    setInterval(updateTime, 1000);
